const Spinner = () => {
  return (
    <span className="inline-block h-5 w-5 border-t-transparent border-solid animate-spin rounded-full border-white border-4"></span>
  );
};

export default Spinner;
